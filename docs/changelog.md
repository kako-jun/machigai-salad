# 変更履歴

## [Unreleased]

### 追加 ✨

- **PNG高画質ダウンロード**
  - プレビューモーダルの「ダウンロード」ボタンでアニメーションPNG形式（フルカラー、最大800px）を生成
  - upng-jsによる可逆圧縮。拡張子.pngで対応ブラウザではアニメーション表示
  - 「シェアする」ボタンは従来通りGIF形式（最大480px、高互換性）
  - ボタンに(GIF)/(PNG)のフォーマットヒントを表示

### 改善 🔧

- **フレーム描画とエンコードの分離**
  - `generateFrames()`: 座標計算・Canvas描画を1箇所に集約しImageData[]を返す共通関数
  - `generateToggleGif()` / `generateToggleApng()`: 同じフレームデータからGIF/APNGにエンコード
  - `GifSource` → `AnimationSource` にリネーム（GIF専用ではなくなったため）
  - 座標ズレの再発が構造的に不可能に

- **GIFディザリングを無効化**
  - `dither: 'FloydSteinberg'` → `dither: false` に変更
  - 256色パレット化時の黒い点々アーティファクトを除去

### 変更 🔧

- **ImageComparison: DOM img廃止、canvas両面描画に移行**
  - 左右両画像をcanvasで描画（DOM `<img>` 要素を廃止）
  - パネルはCSS `aspect-ratio` で画像アスペクト比に追従
  - `imgRect` を `panel.clientWidth/clientHeight` から純粋な数学で計算（DOM img測定に依存しない）
  - ハンドルはDOMで残す（画像外にはみ出してドラッグできるため）

- **GIF生成を表示canvas依存から独立レンダリングに変更**
  - image-utils.ts が画像データURL + displaySize + warpパラメータから独立してフレームを描画
  - `drawMeshWarp` を同じパラメータで呼ぶため、ワープ結果は数学的に同一
  - 表示canvasとGIFの座標ズレが構造的に不可能になった

- **ImageProcessor: leftCanvas/onCanvasReady依存を削除**
  - GIF生成に `leftDataUrl + rightDataUrl + displaySize + warpパラメータ` を渡す方式に変更

### 改善 🔧

- **角ドラッグのUX改善**
  - ドラッグ開始時にコーナーが指の位置にジャンプしなくなった（オフセット記録方式）
  - バイブレーション等による微動を吸収するデッドゾーン（5px）を追加
  - 指を離す瞬間のスリップを防止（200ms前の位置にスナップバック）
  - 角調整画面・比較画面の両方に適用

- **imgRectをパネル寸法から数学的に計算**
  - `panel.clientWidth/clientHeight` からコンテンツ矩形を算出（DOM img測定やobjectFit:containに依存しない）
  - canvas描画のアスペクト比、ハンドル位置が全て正確に

- **ほぞんボタンのフィードバック改善**
  - 保存成功時にボタンが1.5秒間緑チェックマーク+緑色に変化（テキストは「ほぞん」のまま）

- **保存を上書き方式に変更**
  - 同じ画像で何度保存しても前のエントリを上書き（保存枠を消費しない）
  - 新しい画像を読み込んだときだけ新規エントリを作成
  - `updateSave()` を `storage.ts` に追加

### 追加 ✨

- **アニメーションプレビューモーダル**
  - アニメーション生成後にプレビューモーダルを表示（シェア/ダウンロード前に確認）
  - 「シェアする」(GIF)・「ダウンロード」(PNG)の2ボタン
  - SavesPopupと同じモーダルデザイン

- **中心点メッシュワープ**
  - 比較画面に中心ハンドルを追加（四隅+中心の5点変形）
  - 四隅が合っていても中心がずれるレンズ歪みに対応
  - 4三角形アフィン変換でcanvas描画（CSS matrix3dから切替）
  - `lib/mesh-warp.ts` 新規作成
  - GIF生成・保存・復元・アンドゥすべてに中心オフセット反映
  - SaveEntryの`warpCorners`/`centerOffset`を必須化（旧データは`normalize()`で自動補完）

- **画像の自動縮小**
  - カメラ/アルバムから取得した画像を表示解像度×dpr（上限2400px）にcanvasリサイズ
  - メモリ消費・処理速度・LocalStorage保存容量を改善

- **アンドゥ（一手戻し）**
  - 角調整画面・比較画面の両方に「もどす」ボタンを追加
  - ドラッグ操作・再検出の前に自動スナップショット（履歴スタック最大50件）
  - 比較画面では位置オフセット+コーナーワープを一括復元
  - SVGアンドゥアイコン付き

- **SNSシェアボタン**
  - フッターQRコード下にX / LINE / Web Share（またはクリップボードコピー）ボタンを追加
  - Web Share対応端末ではOSシェアシート、非対応ではURLコピーにフォールバック
  - aria-labelをi18n対応

- **比較結果のアニメーションシェア**
  - 比較画面に「アニメーションをつくる」ボタン（btn-action）を追加
  - 左右画像を1秒間隔・無限ループのアニメーションに変換
  - シェア: gif.js（Web Worker）でGIF生成（最大480px、ディザリングなし）
  - ダウンロード: upng-jsでアニメーションPNG生成（最大800px、フルカラー）
  - フレーム描画は共通関数generateFrames()で1箇所に集約
  - スライドoffset・四隅ワープをアニメーションに反映（ユーザーが見ている状態を再現）
  - Web Share API対応端末ではファイル付きシェア、非対応ではGIFダウンロード
  - 二重クリック防止、生成エラー時のトースト通知、30秒タイムアウト

### 改善 🎨

- **Amazonクラッカーアイコン描き直し**
  - パスが途切れていた旧Lucideアイコンを、コーン+破裂線+紙吹雪のシンプルなデザインに描き直し
  - 赤色（`--error`）に変更

- **比較画面ボタンレイアウト改善**
  - ほぞん・シェア・最初からやり直すを同一行に配置
  - シェアは`btn-action`で目立つスタイル

### 変更 🔧

- **リファクタリング**
  - `CornerOffsets` 型を `types/index.ts` に共通化（ImageComparison/ImageProcessor/storageの重複解消）
  - `MAX_UNDO = 50` を共通定数化
  - `UndoIcon / SaveIcon / ShareResultIcon` を `components/icons.tsx` に分離
  - `getImageSize / resizeImage / generateToggleApng` を `lib/image-utils.ts` に分離
  - ImageProcessor.tsx を527行→320行に削減

- **UndoIconをUターン型に変更**
  - リロードアイコンとの混同を防止

- **SWキャッシュ名の自動日付化**
  - next.config.tsでビルド時にsw.jsのキャッシュ名を`machigai-salad-YYYY-MM-DD`に自動置換
  - 手動バージョン管理が不要に

- **感度ラベルのi18n統合**
  - SENSITIVITY_LABELSのハードコード辞書をi18nのt()に統合
  - sensitivityStrict/Normal/Looseキー追加

- **比較画面のコーナーワープ（透視変形）**
  - 左画像の四隅にドラッグ可能なハンドルを追加
  - CSS `matrix3d` によるリアルタイム透視変形で左画像を右画像に合わせられる
  - ドラッグ中は左画像が半透明になり、重なり具合を確認可能
  - スケールスライダーを廃止し、コーナーワープに置き換え
  - `warpCorners` をLocalStorage保存・復元に追加（後方互換、オプショナルフィールド）

- **検出感度の3段階切り替え**
  - 角調整画面の「もう1回けんしゅつ」ボタンで感度を循環切替（きびしめ／ふつう／ゆるめ）
  - 感度ごとにCannyしきい値・epsilon・最小面積・ブラーサイズが異なる
  - 旧「リセット」ボタンを置き換え

- **中央分割線の表示**
  - 角調整キャンバスに白い破線で左右分割の境界線を表示
  - 角のドラッグに連動してリアルタイムに更新

### 変更 🔧

- **比較画面「位置をリセット」→「かどの調整にもどる」に変更**
  - 比較画面から角調整フェーズに戻れるように動線を変更

### 追加 ✨

- **角調整ルーペ（拡大鏡）**
  - ドラッグ中に対角にルーペを表示（3倍ズーム、十字線付き）
  - 指で隠れる角の位置を離れた場所で拡大確認できる
  - 画像端でもクロスヘアが正確にコーナー位置を示す

- **比較画面のドラッグ微調整**
  - 左画像をドラッグして位置オフセット（±40px）
  - ドラッグ中は左画像が半透明になり、右画像が透けて位置合わせ可能
  - タップ（静止）は従来通り右画像表示
  - オフセットリセットボタン付き

- **小さい画像の自動拡大**
  - 角調整・比較画面ともに最低280pxに拡大表示
  - 枠の中に画像が収まらない問題を解消

### 改善 🎨

- **高DPI（Retina）ディスプレイ対応**
  - Canvas描画にdevicePixelRatioを適用
  - ルーペや角ハンドルがシャープに表示される

- **比較画面のビューポート制約**
  - 縦長・横長どちらの画像もビューポート内に収まるよう制約
  - `100dvh`ベースでモバイルのアドレスバーにも追従

- **左右画像のレイヤー構造刷新**
  - 右画像を常時背面配置、左画像をoverlay
  - タップ: 左が150msで全透明へフェード→右が見える
  - ドラッグ: フェード途中で半透明に方向転換→両方重なる
  - すべてのopacity遷移が150ms transitionで滑らかに繋がる

### 変更 🔧

- `drawCanvas`をモジュールレベル関数に抽出（ESLint deps問題解消）
- 角パス描画を`traceCornerPath`ヘルパーに共通化
- イベント処理をPointerEventsに統一（mouse/touch分離を廃止）
- `useCallback`適用（getCanvasPoint, findCornerAtPoint）

- **PWA化**
  - `manifest.webmanifest` + Service Worker（network-first戦略）
  - `display: standalone`、テーマカラー `#FEF6DC`（アイコン背景色と統一）
  - OGPメタタグ（Open Graph + Twitter Card）
  - `ServiceWorkerRegister`コンポーネントで自動登録
  - `PwaInstallPrompt`コンポーネント追加（beforeinstallprompt + appinstalled対応、sessionStorage記憶）

- **画像アセット全7点配置**
  - バナー（800px幅）、OGP（1200x630）、PWAアイコン（192/512）、apple-touch-icon（180）、favicon（32）、QRコード（240、エスプレッソ茶に着色）
  - 全画像webp形式で軽量化

- **ヘッダバナー**
  - ルネサンス絵画風の横長装飾画像をヘッダに追加

- **フッタQRコード**
  - アプリのURLをQRコードで共有できるように（アプリのテーマカラーで着色）

- **LocalStorage保存・復元機能**
  - 比較画面で「ほぞん」ボタンから現在の状態を保存
  - 元画像・4隅座標・ズレ補正量・コーナーワープ・画像サイズを1つのキー(`machigai-salad-saves`)にJSON配列で格納
  - トップ画面に「ほぞんしたやつ (N)」ボタン → ポップアップで一覧表示
  - サムネイル + 日時（自動タイトル）、タップで復元、ゴミ箱で削除
  - 復元時は保存済みの4隅座標から再処理（加工済み画像は保存しない）

- **操作説明テキスト改善**
  - 「タップ→みぎ」→「長おし→みぎ」に修正（実際の操作に合わせた）

- **リセットボタンのレイアウトシフト修正**
  - 条件付きレンダリング → visibility方式に変更、ボタン出現時の画像ずれを解消

- **日英2言語対応**
  - ヘッダ右上にJA/ENトグル。ブラウザ言語で初期値自動判定
  - React Context + 辞書ベースの軽量i18n（lib/i18n.tsx）
  - 全UIテキストをt()関数経由に置換
  - 英語も子供向けフレンドリーなトーン
  - 言語選択はLocalStorageに記憶、html lang属性も動的切替

### 改善 🎨

- **Service Worker をnetwork-first戦略に変更**
  - デプロイ後にユーザーが即座に最新版を取得可能
  - オフライン時のみキャッシュにフォールバック

- **画像出力をwebp形式に変更**
  - toDataURL を PNG → webp/0.85 に変更
  - LocalStorage保存時の容量を大幅削減

- **アクセシビリティ改善**
  - SavesPopup: Escapeキーで閉じる、role="dialog"、aria-modal追加
  - Toast: aria-labelをi18n化

- **OpenCV.js CDNフォールバックを4.x系に統一**
  - 旧1.2.1フォールバックを廃止（API非互換リスク解消）

- **エラー処理強化**
  - FileReader.onerror にトースト通知追加
  - i18nのlocalStorageアクセスをtry-catch保護（Safariプライベート対応）
  - roi Mat の delete順序修正（ダブルフリー防止）

- **ToastアイコンをUnicode→SVG統一**
  - error=警告円、info=チェック円のSVGアイコンに統一

- **ハードコード色のCSS変数化をToastにも適用**

- **landscape対応**
  - `--panel-margin` CSS変数で画像パネルの高さ切替

- **フッターにflex-wrap追加**
  - 375px幅での折り返し対応

- **「ほぞんしたやつ」をImageUpload内にアルバムと対等配置**
  - SaveIcon付き

- **操作説明のmin-height確保**
  - 行数変化によるレイアウトシフト防止

- **削除ボタンaria-label適切化**

- **ファイルサイズ超過時のinputリセット**

- **QRコードをオリーブ色に差替**

## [0.2.0] - 2025-01-XX

### 追加 ✨

- **紙の自動検出機能**
  - OpenCV.jsのCannyエッジ検出で紙の輪郭を抽出
  - 複数のしきい値（30/100, 50/150, 75/200）で試行
  - 4角形の自動検出
  - 検出面積を5%に緩和（従来は10%）

- **台形補正（透視変換）**
  - `getPerspectiveTransform`で変換行列を計算
  - `warpPerspective`で画像を正面視に変換
  - 斜めから撮影した画像にも対応

- **角の調整UIコンポーネント**
  - 自動検出した4つの角をオレンジ色の点で表示
  - マウス/タッチでドラッグして微調整可能
  - ドラッグ中の角が光るグロー効果
  - 方向を示す矢印絵文字（↖️↗️↘️↙️）
  - リセット、キャンセル、適用の3つのボタン

### 改善 🎨

- **子供向けUIに全面刷新**
  - 専門用語を平易な言葉に変更
    - 「紙の範囲を調整」→「📄 まっすぐに直そう！」
    - 「適用」→「これでOK！」
    - 「別の画像を読み込む」→「🔄 別の絵で遊ぶ」
  - 絵文字を多用（🍤🎯📷💡など）
  - オレンジ〜イエローのグラデーション背景
  - ボタンにタップアニメーション（active:scale-95）

- **エラーメッセージの改善**
  - 「画像の処理中にエラーが発生しました」
    → 「😅 うまくいかなかったみたい。もう一度写真をとってみてね！」
  - ローディング: 「🔍 絵をチェック中... ちょっと待ってね！」

- **認識精度の向上**
  - 複数のepsilon値（0.02, 0.03, 0.04）で四角形近似を試行
  - 良い候補（20%以上）が見つかったら早期終了
  - より多くのケースで紙を検出可能に

- **角の調整UIのビジュアル改善**
  - 太い線（lineWidth: 3）で見やすく
  - 影付きで立体感を演出
  - 選択中の角を明るく表示
  - 白い縁取りでコントラスト向上

### 変更 🔧

- 画像処理フローを2段階に分割
  1. `detectPaperCorners()`: 紙の検出
  2. `processImageWithCorners()`: 台形補正〜分割

- 新しいコンポーネント追加
  - `PaperCornersAdjustment.tsx`: 角の調整UI

- 状態管理の追加
  - `detectedCorners`: 検出した4つの角の座標
  - `showCornersAdjustment`: 調整UI表示フラグ

## [0.1.0] - 2025-01-XX

### 追加 ✨

- **初期リリース**
  - カメラで撮影 / ファイルから画像を選択
  - OpenCV.jsの動的ロード
  - 色調補正（ヒストグラム均等化）
  - 画像の左右分割
  - タップで画像を切り替えて比較
  - レスポンシブデザイン
  - Next.js 15で静的サイトとしてビルド
  - GitHub Pagesへの自動デプロイ

### コンポーネント

- `ImageProcessor.tsx`: 画像処理の統合コンポーネント
- `ImageUpload.tsx`: 画像アップロードUI
- `ImageComparison.tsx`: 左右画像の比較UI

### 技術スタック

- Next.js 15
- React 19
- TypeScript
- Tailwind CSS
- OpenCV.js 4.9.0

## リリースノート形式

### バージョン表記

- メジャー: 大きな機能追加や破壊的変更
- マイナー: 機能追加や改善
- パッチ: バグフィックスや小さな修正

### カテゴリ

- ✨ 追加 (Added): 新機能
- 🎨 改善 (Improved): 既存機能の改善
- 🔧 変更 (Changed): 動作の変更
- 🐛 修正 (Fixed): バグフィックス
- 🗑️ 削除 (Removed): 機能の削除
- 🔒 セキュリティ (Security): 脆弱性対応
- 📚 ドキュメント (Documentation): ドキュメント更新

## 今後の予定

### v1.0.0（目標）

- 実機テスト完了
- ESLint 8→9移行
- パフォーマンス最適化
- 完全なドキュメント

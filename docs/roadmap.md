# ロードマップ

現在の実装とコードのコメント・TODOリストから読み取れる、今後の方向性をまとめる。

---

## 近い将来（次のリリース候補）

### 差分の自動ハイライト

左右の画像の差分を計算して、違う箇所を視覚的に強調表示する機能。
「タップで切り替えて自分で探す」を補助する形で、ヒントのON/OFFを切り替えられる仕組みを想定している。
実験的機能として実装し、精度を見ながら改善していく方針。

### 検出失敗時のフィードバック改善

紙の輪郭が検出できなかった場合、現状は静かに次のステップに進む。
「明るい場所で撮ってみて」「もう少し近づいて」といった具体的なヒントをユーザーに伝えられるよう改善したい。
加えて、自動検出に失敗したとき手動で4つの角を一から指定できるモードの追加も候補にある。

### 角調整UIの操作性改善

~~ピンチズームで拡大~~ → **ルーペ（拡大鏡）で実装済み**。ドラッグ中に対角に3倍ズームの拡大鏡が表示され、指で隠れる角の位置を確認できる。
さらに比較画面でも左画像のドラッグ微調整が可能になり、角調整に戻らなくても位置補正できる。

---

## 中期的な改善

### 処理中の視覚フィードバック

画像処理の間、元画像をぼかして背景に表示したり、進捗を％で示したりすることで、
「止まっているのか処理中なのか」がわかりやすくなる。特に重い処理をするときに有効。

### ~~画像保存・シェア機能~~ → **実装済み**

LocalStorage保存（最大5件）、アニメーションGIF生成+SNSシェア、X/LINE/Web Shareボタンを実装。

### OpenCV.jsのローカルホスティング

現在はCDNから読み込んでいるが、ビルド時に含めることでCDN障害時のフォールバックになる。
初回ロード速度の改善にも寄与する。

---

## 長期的な可能性

### ~~多言語対応~~ → **実装済み**

日本語・英語の2言語対応を実装。ブラウザ言語で自動切替、ヘッダトグルで手動切替も可能。

### ダークモード

システム設定に追従するダークモード対応。
夜間使用時の目への負担を軽減できる。

### コラボレーション・ゲーミフィケーション

友達と一緒に間違い探しを楽しむ機能や、タイムアタックモードなど。
現時点では「いつかやりたいアイデア」の段階で、実装は後回しにしている。

### ブラウザ拡張機能

Webページ上の画像に直接この機能を適用できるChrome拡張機能。
Webページ上の間違い探し画像に直接適用できるようにするアイデア。

---

## 現在完了済みの機能

- カメラ撮影・ファイル選択
- 紙の自動検出（Cannyエッジ検出、複数しきい値試行）
- 台形補正（透視変換）
- 角の手動調整UI（タッチ・マウス対応、グロー効果）
- 左右分割
- タップ切り替え比較（半透明オーバーレイ）
- 比較画面のドラッグ微調整（±40px位置オフセット）
- 角調整ルーペ（3倍ズーム拡大鏡）
- 高DPI（Retina）ディスプレイ対応
- 小画像の自動拡大（長辺280px未満）
- ビューポート制約（縦長/横長画像対応）
- Cloudflare PagesへのGit連携自動デプロイ
- PWA化（manifest.webmanifest + Service Worker + OGP）
- ヘッダバナー（ルネサンス絵画風装飾）
- フッタQRコード共有
- コーナーワープ（CSS matrix3d 透視変形）
- 検出感度の3段階切り替え（きびしめ/ふつう/あまめ）
- 中央分割線の表示
- 日英2言語対応（React Context + 辞書ベースi18n）
- LocalStorage保存・復元（最大5件）
- 画像の自動縮小（表示解像度×dpr、上限2400px）
- アンドゥ（角調整+比較画面、履歴スタック最大50件）
- SNSシェアボタン（X / LINE / Web Share / クリップボード）
- 比較結果GIFシェア（gif.js、Floyd-Steinbergディザリング、offset/warp反映）
- Nostalgicカウンター + ビルド日バージョン表示
